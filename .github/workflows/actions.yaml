name: Actions Workflow

on:
  push:
    branches:
      - master
      - develop
      - 'release/*' # match: release/1.0.0, not match release/1.0.0/beta
      - 'release/**' # match all release/xxxx/xxx

jobs:
  run-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: List Files 1
        run: |
          pwd
          ls -a
          whoami
